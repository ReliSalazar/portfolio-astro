---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<section>
  {
    title && (
      <h2>
        &lt;<span>λ</span> {title} /&gt;
      </h2>
    )
  }
  <slot />
</section>

<style>
  section {
    max-width: 700px;
    margin: 0 auto 48px;
  }

  h2 {
    margin-bottom: 8px;
    font-weight: 700;
    line-height: 1.5;
    font-size: 1.5rem;
  }

  span {
    color: var(--accent-color);
  }
</style>
